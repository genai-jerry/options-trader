import { Typography } from '@mui/material';

export function ZerodhaSync() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Zerodha Sync
      </Typography>
      <Typography color="text.secondary">
        OAuth, live funds / holdings / positions / orderbook (read-only) — Step 11.
      </Typography>
    </>
  );
}
