import { Typography } from '@mui/material';

export function Withdrawals() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Withdrawals
      </Typography>
      <Typography color="text.secondary">Pending / Confirmed / Cancelled tabs — Step 8.</Typography>
    </>
  );
}
