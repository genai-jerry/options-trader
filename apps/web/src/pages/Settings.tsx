import { Typography } from '@mui/material';

export function Settings() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Settings
      </Typography>
      <Typography color="text.secondary">
        principalX, fees, position-size cap, AI toggle, Zerodha credentials — Step 5.
      </Typography>
    </>
  );
}
